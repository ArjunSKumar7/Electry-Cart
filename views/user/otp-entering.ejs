<section>
    <div class="page-header breadcrumb-wrap">
      <div class="container">
        <div class="breadcrumb">
          <a href="/" rel="nofollow">Home</a>
          <span></span> Pages <span></span> Login / Register
        </div>
      </div>
    </div>
    <main class="main">
      <section class="p-auto my-5">
        <div class="container">
          <div class="row">
            <div class="col-lg-10 m-auto">
              <div class="row d-flex justify-content-center">
                <div class="col-lg-6">
                  <div
                    class="login_wrap widget-taber-content p-30 background-white border-radius-10 mb-md-5 mb-lg-0 mb-sm-5"
                  >
                    <div class="padding_eight_all bg-white">
                      <div class="heading_s1">
                        <h5 class="mb-30">OTP</h5>
                      </div>
                      <form action="/verifyotp" method="post">
                        <div class="form-group">
                          <input type="" required="" name="otp" placeholder="" />
                        </div>
  
                        <div class="form-group">
                          <button
                            type="submit"
                            class="btn btn-fill-out btn-block hover-up"
                            name="login"
                            style="width: 100%"
                          >
                            Submit 
                          </button>
                        </form>
                          <form action="/postotp" method="POST">
                          <button class="btn" id="resend-otp-btn" style="display: none;">Resend OTP</button>

                          </form>
                          <p id="countdown"></p>
                        </div>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>
    </main>
  </section>
  
  
  <script>
    let countdown = 60;
  let intervalHandle;
  function startCountdown() {
      intervalHandle = setInterval(() => {
          countdown--;
          document.getElementById('countdown').innerHTML = `Resend OTP in ${countdown} seconds`;
          if (countdown === 0) {
              clearInterval(intervalHandle);
          document.getElementById('countdown').innerHTML ='';
          document.getElementById('resend-otp-btn').style.display = 'block';
          countdown=60;
  
              document.getElementById('resend-otp-btn').disabled = false;
          }
      }, 1000);
  }
  window.onload=startCountdown;
  
  // Call the function when the user requests a new OTP
  // document.getElementById('resend-otp-btn').addEventListener('click', () => {
  //     document.getElementById('resend-otp-btn').style.backgroundColor = '#fff';
  //     document.getElementById('resend-otp-btn').style.color = '#000';
  //     startCountdown();
  // });
  </script>